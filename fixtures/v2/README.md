# Rejection-only v2 inputs

This directory is not an active contract and is never loaded by the product
runtime. It contains only the five old main-record shapes needed by the schema
gate to prove that v3 rejects v2 data instead of migrating or filling fields.

Do not add positive lifecycle examples, migration reports, auxiliary artifacts,
or legacy Version sources here. The complete pre-cutover evidence belongs only
to the read-only release backup.
