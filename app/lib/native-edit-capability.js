// Canvas and the Repository save boundary must reject the same text hosts.
// Keep this renderer façade so existing imports do not create a second list.
export { isNativeDirectEditRoot } from "../../shared/native-edit-capability.mjs";
